# ----------------------
# Required credentials
# ----------------------
# Last.fm API key used for global play-count lookups.
LASTFM_API_KEY=

# ----------------------
# Navidrome API
# ----------------------
# Base URL of the Navidrome server (e.g., https://music.example.com).
SUBSONIC_URL=
# Username used to authenticate API calls.
SUBSONIC_USER=
# Preferred: provide your password and let the script derive tokens automatically.
SUBSONIC_PASSWORD=
# Alternative: leave SUBSONIC_PASSWORD blank and provide a precomputed token+salt pair.
SUBSONIC_TOKEN=
SUBSONIC_SALT=
# Optional overrides; defaults match the script if left blank.
SUBSONIC_API_VERSION=1.16.1
SUBSONIC_CLIENT=nd-star-script

# ----------------------
# Library and playlists
# ----------------------
# Root of the on-disk music collection.
MUSIC_ROOT=/Volumes/NAS/Media/Music/Music_Server/
# Directory that stores generated playlists.
PLAYLIST_DIR=/Volumes/NAS/Media/Music/Music_Server/_playlists/
# Output filename for the top-artist playlist.
PLAYLIST_NAME=top_artist_tracks.m3u
# Output path for the non-FLAC playlist.
NONFLAC_PLAYLIST_PATH=/Volumes/NAS/Media/Music/Music_Server/_playlists/Look For Replacment.m3u

# ----------------------
# Path rewriting
# ----------------------
# Absolute prefix to replace when emitting playlist entries.
PATH_REWRITE_FROM=/Volumes/NAS/Media/Music/Music_Server/
# Replacement prefix used by players/containers.
PATH_REWRITE_TO=/music/

# ----------------------
# Last.fm client tuning
# ----------------------
# Max requests per second when calling Last.fm.
LASTFM_REQUESTS_PER_SEC=4
# Worker threads for concurrent Last.fm lookups.
MAX_WORKERS=8
# JSON cache for Last.fm responses (relative paths resolve against the repo).
LASTFM_CACHE_FILE=.lastfm_cache.json

# ----------------------
# CSV-to-M3U matching
# ----------------------
# Default CSV file to load if no argument is provided.
CSV_INPUT_FILENAME=Liked_Tracks_clean.csv
# RapidFuzz score required to accept a match.
FUZZY_MATCH_THRESHOLD=86
# Component weights for composite RapidFuzz scoring.
FUZZY_TITLE_WEIGHT=0.60
FUZZY_ARTIST_WEIGHT=0.30
FUZZY_ALBUM_WEIGHT=0.10
# Skip metadata reads for raw PCM files (wav/aiff).
SKIP_RAW_PCM_TAGS=true

# ----------------------
# Logging
# ----------------------
# Baseline logging level; use INFO, DEBUG, WARNING, etc.
LOG_LEVEL=INFO
